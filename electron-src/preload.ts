/**
 * Preload script — intentionally minimal.
 *
 * The Evolve Selector app runs entirely in the renderer with no need
 * for Node.js or Electron IPC. This file exists as a placeholder for
 * future extensibility (e.g., file export or system-level features).
 */
